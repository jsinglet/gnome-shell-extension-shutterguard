{
  "private": true,
  "scripts": {
    "format:generated": "eslint --fix \"build/compiled/*.js\" && prettier --write \"build/compiled/*.js\"",
    "build": "tsc -p tsconfig.json && eslint --fix \"build/compiled/*.js\""
  },
  "devDependencies": {
    "@girs/gjs": "latest",
    "@girs/gnome-shell": "latest",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.7.0",
    "typescript": "latest"
  }
}
